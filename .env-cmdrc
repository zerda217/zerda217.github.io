{
  "development": {
    "REACT_APP_KAKAO_APP_KEY": "$(cat secret.json | jq -r .KAKAO_APP_KEY)"
  },
  "production": {
    "REACT_APP_KAKAO_APP_KEY": "$(cat secret.json | jq -r .KAKAO_APP_KEY)"
  }
}
